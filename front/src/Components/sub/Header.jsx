import React from "react";
import { Link } from "react-router-dom";

const Header = () => (
	<div className="c_header_dv">
		<Link to="/create">Ask a question</Link>
	</div>
);

export default Header;
